# wango Origin — Hero Site

Scroll-driven product hero for the **wango Origin WOB** earbuds.

- `index.html` — self-contained page (Plus Jakarta Sans + Quicksand via Google Fonts)
- `assets/` — alpha (VP9 WebM) + MP4 fallback videos: main reveal & exploded view

Static site — deploy anywhere (Vercel: zero-config).
